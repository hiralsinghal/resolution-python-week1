[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "resolution_week1_hiralsinghal"
version = "1.0.0"
dependencies = []
license = "MIT"

[project.scripts]
todo = "resolution_week1_hiralsinghal.main:main"